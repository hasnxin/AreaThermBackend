package com.areatherm.thermal.model;

/** A shelter's front-face orientation. Mirrors engine.js's ORIENT_OFFSET keys. */
public enum CompassOrientation {
    NORTH, SOUTH, EAST, WEST, NE, NW, SE, SW, CUSTOM
}
