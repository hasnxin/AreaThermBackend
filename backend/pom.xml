<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.areatherm</groupId>
  <artifactId>backend</artifactId>
  <version>0.1.0</version>
  <name>AreaTherm Backend</name>
  <description>
    Production backend for AreaTherm (passive-shelter thermal design).
    Infrastructure only in this pass, no LLM wired up yet (see ml/ package).
    thermal/ and optimization/ are a numerically-verified port of the existing
    app/js/engine.js physics/optimizer, see src/test/resources/physics for
    the golden-file fixtures that prove fidelity to the JS engine.
  </description>

  <properties>
    <java.version>17</java.version>
    <jjwt.version>0.13.0</jjwt.version>
    <!-- Pinned to 2.0.5 deliberately, not the newest release: OpenPDF 3.x
         renamed its packages from com.lowagie.text to org.openpdf.text,
         which flying-saucer-pdf 9.4.0 (compiled against the legacy
         namespace) cannot resolve; and 2.1.0+ compiles to Java 21
         bytecode (class file major version 65), incompatible with this
         project's Java 17 target. 2.0.5 is the newest release satisfying
         both constraints, verified directly against each jar's actual
         class files, not assumed from version numbers. -->
    <openpdf.version>2.0.5</openpdf.version>
    <flyingsaucer.version>9.4.0</flyingsaucer.version>
    <springdoc.version>2.9.1</springdoc.version>
    <archunit.version>1.5.0</archunit.version>
    <netcdf.version>5.9.0</netcdf.version>
  </properties>

  <repositories>
    <!-- Unidata's netCDF-Java (cdm-core, below) isn't on Maven Central. -->
    <repository>
      <id>unidata-all</id>
      <url>https://artifacts.unidata.ucar.edu/repository/unidata-all/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-cache</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <!-- Boilerplate reduction for JPA entities (getters/setters/no-args ctor).
         Version is managed by the spring-boot-starter-parent BOM. -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- JWT -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- PDF reports: Flying Saucer (HTML/CSS layout) rendering via OpenPDF (Apache-2.0) -->
    <dependency>
      <groupId>com.github.librepdf</groupId>
      <artifactId>openpdf</artifactId>
      <version>${openpdf.version}</version>
    </dependency>
    <dependency>
      <groupId>org.xhtmlrenderer</groupId>
      <artifactId>flying-saucer-pdf</artifactId>
      <version>${flyingsaucer.version}</version>
    </dependency>

    <!-- API docs; doubles as a running check that controllers match API_SPEC.md -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- ERA5 data comes back from Copernicus CDS as NetCDF; see climate/era5/.
         guava is cdm-core's own transitive dependency (its public API, e.g.
         NetcdfFile.getVariables(), returns Guava collection types directly)
         but isn't reliably pulled onto the compile classpath on its own. -->
    <dependency>
      <groupId>edu.ucar</groupId>
      <artifactId>cdm-core</artifactId>
      <version>${netcdf.version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>33.3.1-jre</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>areatherm-backend</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
