.DS_Store
Thumbs.db

# Large, regeneratable intermediate for the ML surrogate — the trained
# output (app/js/ml-model-data.js) is what ships; the raw training rows are
# rebuilt on demand via: node tools/generate-training-data.js
tools/training-data.json
